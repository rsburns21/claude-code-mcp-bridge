ANTHROPIC_API_KEY=replace_me
PORT=3000
# Optional: restrict Claude tools at the SDK layer (comma-separated)
CC_ALLOWED_TOOLS=Bash,Read,Git
CC_DISALLOWED_TOOLS=
# Optional: working directory default for sessions
# On Windows this can be something like C:\Users\<you>\work  (escape backslashes)
CC_CWD=/srv/workdir